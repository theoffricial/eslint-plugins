// the tested unit should be imported
import '';

describe('test file example for eslint-plugin-jest', () => {
    it.todo('case 1');
    it('case 2', () => {
        expect(1).toBe(1);
    });
    it.todo('case 3');
});
